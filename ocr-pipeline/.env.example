# DriftMoney OCR Pipeline - Environment Configuration

# LLM Model to use
LLM_MODEL=llama3.1:8b

# Caddy domains (must have DNS A records pointing to this server)
API_DOMAIN=api.yourdomain.com
OLLAMA_DOMAIN=ollama.yourdomain.com

# Basic auth credentials
API_USER=apiuser

# Generate with: docker run --rm -it caddy:2 caddy hash-password
API_PASSWORD_HASH=
